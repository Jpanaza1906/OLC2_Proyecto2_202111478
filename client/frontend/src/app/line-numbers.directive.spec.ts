import { LineNumbersDirective } from './line-numbers.directive';

describe('LineNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new LineNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
